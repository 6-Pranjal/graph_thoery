name: Firdt Workflow
on:
  push:
    branches:
      - "*"
  pull_request:
    branches:
      - "*"

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Run s simple command
        run: echo "github!"
      
      
  
